# 引き継ぎ（2店舗目の立ち上げ）

作業を別のセッション・別の担当へ引き継ぐための現在地。**日付順の記録ではなく、
「いまどこまで進んでいて、次に何をするか」だけを書く。** 終わった項目は消してよい。

サーバーのホスト名・IP・認証情報は**このファイルに書かない**（リポジトリは公開設定のため）。
引き継ぎ時に別途伝える。

> **リポジトリの移管（2026-08）**: FREE WAN 店（2店舗目）のアプリは
> **emcyrup/freewan で管理する**ことになった。デプロイの Secrets もこのリポジトリに置く。
> cocotte-vert（1号店）は元リポジトリのまま。汎用的な修正は upstream 経由で相互に取り込む
> （README の手順参照）。
>
> これに伴うサーバー側の切り替え（1回だけ・データは消えない）:
>
> ```bash
> cd ~/cocotte-vert
> git remote set-url origin https://github.com/emcyrup/freewan.git
> git fetch origin && git checkout main && git reset --hard origin/main
> docker compose --profile standalone up -d --build
> ```
>
> ディレクトリ名は `cocotte-vert` のままでよい（CI は freewan / cocotte-vert 両対応）。
> Docker のボリューム名はディレクトリ名から決まるため、**ディレクトリを rename しないこと**
> （rename すると新しい空の DB で起動してしまう）。

---

## 1. 2店舗目のサーバー構築

GCP に VM を1台立て、`docs/new-store.md` の手順で構築中。

| Step | 内容 | 状態 |
|---|---|---|
| 1 | VM を用意（固定 IP・ポート80/443 開放） | **完了** |
| 2 | ドメインを DNS で VM に向ける（DuckDNS） | **完了** |
| 3 | Docker とコードを入れる | **完了** |
| 4 | `.env` を作る | **完了**（LINE 本物の値に差し替え済み） |
| 5 | 起動して HTTPS を確認・管理画面が開く | **完了** |
| 6 | LINE チャネル・LIFF を作り Webhook を設定 | **ほぼ完了**（2026-08-13 check-line.js 全項目OK: Bot応答・Webhook検証200・LIFF設定済み。残: リッチメニュー登録と、スタッフ用 LINE グループに Bot を招待） |
| 7 | 初期データ投入（メニュー・コース・スタッフ・既存顧客） | **進行中**（2026-08-13 メニュー9件・スクール会員コース2件を投入済み。残: スタッフ登録と LINE 連携、既存顧客台帳、`.env` の `STORE_*`） |
| 8 | 自動デプロイを2台目に対応させる | **完了**（2026-08-13 疎通確認済み。main への push で自動反映される） |

### Step 6 の残り

1. リッチメニュー登録（`scripts/setup-richmenu.js`。画像 2500×843px が要る）
2. スタッフ用 LINE グループを作り Bot を招待する（`STAFF_NOTIFY_CHANNEL=line` のため、
   招待するまでスタッフ通知の届き先が無い。招待すると通知先グループが自動設定される）
3. 現在のチャネルは表示名「freewanテスト」。本番の公式アカウントに切り替える際は
   `docs/switch-account.md` の手順で（**同一プロバイダー内**でないと友だちの紐付けが切れる点に注意）
4. 通数は無料枠 200通/月。live 切り替え前に想定通数を試算してプランを上げる

### Step 7 の残り

- メニュー・コースは投入済み（所要時間はモック段階の仮値。ヒアリング後に
  `scripts/store-data/freewan.menus.json` を直して再実行。同名はスキップされるので何度でも安全）
- スタッフを管理画面から登録し、LINE 連携する（`docs/shift-requests.md`）
- **既存顧客台帳（氏名・電話番号）の投入が最重要。** LIFF 登録時の突合率に直結する
- `STORE_*` の残り（営業時間・定休日・住所・電話・サブタイトル）を `.env` に追記する。
  未設定だと現行店舗の既定値が出る
- **`ANTHROPIC_API_KEY` を `.env` に追記する**（2026-08-14 未設定と判明）。無いと
  スタッフのシフト変更申請が解釈されず（申請が1件も作られない）、来店フォロー返信の
  自動分類も動かない。起動ログに警告が出るようにしてある
- 実データが入るまで画面を空のまま触るのが辛いときは、デモ用の顧客・予約を投入できる
  （`node scripts/seed-customers.js` → `node scripts/seed-reservations.js`。どちらも `--remove`）。
  予約サービスを通さない直接 INSERT で、LINE 連携済みの顧客にはぶら下げないため、
  `SEND_MODE=live` のままでもスタッフ通知・顧客配信は発生しない

### Step 8 でやること（残りは Secrets 登録のみ）

ci.yml は複数台対応済み。`DEPLOY_TARGETS`（`user@host` の改行区切り）で全店舗へ配る。
残作業（リポジトリの Settings → Secrets and variables → Actions）:

1. 1台目と同じ deploy_key の**公開鍵を2台目にも登録**（GCP なので `authorized_keys` 直接追記ではなく
   **インスタンスのメタデータ**に追加する。手順と注意は docs/deploy.md「GCP では〜」参照）
2. Secret `DEPLOY_TARGETS` に2台ぶんの `user@host` を改行区切りで登録
   （設定後は旧 `VM_HOST` / `VM_USER` は使われないので消してよい）
3. main に何かをマージし、両店舗へ配られることを Actions のログで確認
   （1台の失敗で他店舗は止まらず、最後にまとめて失敗報告される）

**店舗ごとにコードを分岐させないこと。** 差分は `.env` に寄せる方針で作ってある
（`src/store.js` と配信日数の設定）。メニュー・定額コースの初期データも
`scripts/store-data/` の JSON に寄せた。どうしても吸収できない差が出たら、まず設定化を検討する。

---

## 2. 本稼働までの残り作業（北区店の要件 R1〜R10 に対して）

> **番号の読み替えに注意**: 画面・API に出す R番号は**要件書**（`docs/osaka-kitaku/要件定義書.md`）に
> 合わせてある（2026-08-14 に振り直し済み）。ただしジョブの**保存キー**（message_logs.job_type や
> customer_reminder_settings.job に残る値）は実装順の歴史的な名前のままで、
> 例えば R10 休眠フォローのキーは `dormant`、R4 来店フォローのキーは `afterVisit`。
> 対応表の正は `src/reminders.js` の `REMINDER_JOBS`。

### 配信（リマインド）

| 要件 | システム側の状態 |
|---|---|
| R1 前々日確認 | **実装済み**（preReminder。店舗・お客様単位の ON/OFF 付き） |
| R2 誕生日祝い | **実装済み**（birthday。クーポンは `BIRTHDAY_COUPON_URL`） |
| R3 ワクチン更新案内 | **実装済み**（2026-08-14。vaccine・接種日＋1年の期限が30日以内＋直近半年の利用者。スタッフにも一覧を通知。接種日はカルテの「わんちゃんの情報を編集」から入力） |
| R4 体験3日後の体調伺い | **部分**。afterVisit の日数は `.env` で 7→3 にできるが、「体験利用のみ対象」の絞りが無い |
| R5 体験後の追客（3パターン） | **未**（入園状況のステータス管理ごと未着手） |
| R6 回数券の来店促し | **実装済み**（2026-08-14。ticketNudge・14日未来店＋期限内の残ありで送信） |
| R7 会員の残回数案内 | **実装済み**（2026-08-14。planNudge・加入中のみ・7日未来店で送信） |
| R8 持ち越し分の消化案内 | **実装済み**（2026-08-14。carryNudge・当月中に失効する繰越分がある方へ月1回。当月分優先消化のため、先の予約があっても除外しない） |
| R9 来店お礼（写真付き） | **実装済み**（2026-08-14。thanks・当日来店で写真を登録した予約へ 19:00。予約一覧の「写真」ボタンから最大4枚。文1通＋写真4枚＝課金1通分） |
| R10 休眠フォロー（90日） | **実装済み・採用決定**（2026-08-14。要件書に R10 として追記済み。管理画面の ON/OFF で制御） |
| スタッフ確認付き送信 | **実装済み**（2026-08-14。管理画面で auto/manual を切替。manual では承認したものだけ送信） |

### 配信以外（要件書 2.1／2.2／2.4／2.5 の項目ごと）

| 要件 | 状態 |
|---|---|
| 2.1 カルテ・スクール記録の一元管理 | 実装済み |
| 2.1 来店履歴 | 実装済み（予約を来店済にすると履歴と最終来店日が残る） |
| 2.1 **来店経路**（EPARK / 電話 / EPARK TEL / 公式LINE） | **未**。DB に列が無く、画面の集計はサンプル値。要件書 2.1・2.4 の両方に関わる |
| 2.1 ワクチン更新の自動抽出＋スタッフ通知 | 実装済み（R3。通知先は Slack / LINE グループ。**メール送信は未実装**だが要件は「メール**または**LINE」のため充足） |
| 2.1 スクールの月内利用回数を顧客ページに表示 | 実装済み（カルテの「ペットスクールの利用」に 当月 used / quota と繰越を表示） |
| 2.3 失効した回数の履歴・累計失効回数 | 実装済み（カルテの「失効した回数」。失効日／内訳／回数） |
| 2.2 シフト区分（AM半休・PM半休・公休・有休・数時間休）の選択入力 | 実装済み（`shift_kind`。マスのクリックで巡回、時間休は時刻指定） |
| 2.2 シフト管理＋公式LINE からの申請・承認・本人通知 | 実装済み（`ANTHROPIC_API_KEY` が必要） |
| 2.2 予約カレンダーにシフトを反映 | モック上は反映。**実データ化は未** |
| 回数券・会員管理 | 実装済み（`docs/plans.md`）。**来店登録との連動も実装済み**（2026-08-14。予約に `pet_id` を持ち、visited で自動消化・取り消しで復元。対象メニューは `menus.consumes` で設定） |
| 予約カレンダー（日/週/月・ドラッグコピー・両端リサイズ・時間帯色分け・新規+30分枠） | **未**（画面はサンプルのまま。仕様は要件書 2.2 と `docs/osaka-kitaku/mock/` を正とする）。2026-08-14 に、複数頭が入る帯をタップするとわんちゃん一覧が出て、選ぶとカルテが開くようにした（早く来る子・遅くまでいる子は⭐と時刻で表示）。実データ化のときも同じ形を保つこと |
| 新人スタッフの⭐マーク | **未**（モック画面のみ。実装の staff には無い） |
| EPARK メール自動取込 | 未。Phase 1 で実物を確認してから着手（それまで工数が読めない） |
| 2.4 ダッシュボード（当月入園数・体験入園数・当月来園数） | **未**。画面はサンプル値。入園数は 2026-08-14 に入れた `reservations.school_stage`、来園数は visited の予約から出せる状態にはなっている |
| 2.4 トリマー実績（施術数・新規/既存・貢献売上） | **未**。担当（`staff_id`）は持っているが、新規/既存の判定と売上を持っていない（売上は Airレジ CSV の取り込みが前提） |
| 2.5 SNS 2分割投稿（10枚超） | 実装済み（Instagram・スレッズ） |
| 2.5 リール投稿 | **未** |
| 顧客ごとのリマインド設定 | 実装済み（R5 以外の全ジョブが対象） |
| テスト送信の対象 | **部分**。R1 前々日 / R4 来店フォロー / R2 誕生日 / R10 休眠 と予約の3通知のみ。**R3・R6・R7・R8・R9 の文面はテスト送信できない**（`adminRoutes.js` の `/test-message`）。live 運用中に文面を確かめる手段が dry_run の標準出力しかないため、追加を検討する |

### 次に着手すべきもの

1. 予約カレンダーの実データ化（いまはサンプル。R9 の写真登録は予約一覧側にあるため、
   カレンダーからも登録できるようにするのはこの後）
2. リール投稿
3. EPARK メール自動取込（実物を確認してから）
- R4 の「体験のみ」絞り込みと R5 追客は、入園ステータスの持ち方を店舗と確認してから設計する

（済）R8 / R3 / R9 — 2026-08-14 実装。手動確認は
`node scripts/run-job.js --job=carryNudge --dry-run`（vaccine / thanks も同様）。
R9 は写真の公開 URL が要るため `PUBLIC_BASE_URL`（未設定なら `DOMAIN` から導出）が必須で、
未設定のときは対象を数えず何も送らない。写真は `data/thanks`（Docker のボリュームで永続化）。
マイグレーション 013〜015 はコンテナ起動時に自動適用される。

（済）用語の統一 — 2026-08-14。1号店の「保育コース」を北区店の呼び方
**「ペットスクール」**へ置換し、「一時預かり」の記載を削除した。
**投入済みの DB には反映されない**ため、管理画面のメニュー編集で
「幼稚園（スクール）」→「ペットスクール」に改称し、「一時預かり」を無効にすること
（回数消化はメニュー名で引くので、改称前に入っている先の予約は新しい名前に直す）。

（済）スタッフ確認付き送信 — 2026-08-14 実装。リマインド画面に「スタッフ確認付き送信」カードを追加。
auto（自動送信・既定）/ manual（承認後に送信）を切替でき、manual では毎朝のジョブが
pending_deliveries に積まれ、承認したものだけが送信される（見送りも可・一括承認あり）。
SEND_MODE の3段階ガードは承認後の送信にもそのまま効く。手動実行（run-job.js）も同じ挙動。
試験運用（Phase 5）では manual + SEND_MODE=test で文面と対象を確かめてから live へ。

（済）R6 / R7 の配信条件 — 2026-08-14 実装。dry_run のまま毎朝10:00に対象抽出される。
手動確認: `node scripts/run-job.js --job=ticketNudge --dry-run`（planNudge も同様）

（済）来店登録と回数消化の連動 — 2026-08-14 実装。予約フォーム・手入力・取り込み API とも
「どの子か」を受け取り、visited への変更で `menus.consumes`（plan/ticket）に応じて1回消化。
残0で消化できないときは、コース加入中の場合のみスタッフへ通知。来店の取り消しで消化も戻る。
マイグレーション 011 はコンテナ起動時に自動適用される。残る手作業は
`seed-menus.js --file=...` の再実行（メニューへの consumes 反映）のみ。

### 既知の指摘（未解消）

- Phase 2 の API 申請に**スレッズ**が含まれていない（Instagram とは別トークンが要る）

---

## 3. 店舗へ確認したい運用ルール

回数券・ペットスクールは現行モックの表示に合わせて実装してある。店舗ごとに違いうるので、
ヒアリング（Phase 1）で確認する。変更はいずれも1箇所で済むようにしてある。

- 消化の順番は**当月分優先**でよいか（繰越分が失効しやすくなる）
- 繰越は**1ヶ月**でよいか
- 月途中の加入は**満額**か日割りか（いまは満額）
- 解約後の残回数は**期限まで使えるまま**でよいか
- 早朝・夜間の基準時刻（画面では 10:00 / 18:00 と仮置き）と早朝・夜間料金の有無
- 回数消化の対象メニュー（現在: ペットスクール=コース消化、シャンプーコース=回数券消化。
  `scripts/store-data/freewan.menus.json` の `consumes` で変更できる）

---

## 4. 引き継ぐときに必ず伝えること

このファイルに書かない情報。口頭か別の安全な経路で渡す。

- 2店舗目のドメイン・外部 IP・SSH のユーザー名
- 管理画面の `ADMIN_USER` / `ADMIN_PASSWORD`
- LINE / Instagram / Threads / Anthropic の各トークン
- `POSTGRES_PASSWORD`

---

## 5. 作業を引き継いだ人が最初に読むもの

| | |
|---|---|
| 開発の約束ごと | `CLAUDE.md`（**誤爆防止のルールは必ず読む**） |
| 全体像 | `README.md` |
| 別店舗への展開手順 | `docs/new-store.md` |
| サーバー構築 | `docs/deploy.md` |
| 画面の切り分け（実運用／デモ） | `src/mock/README.md` |
| 回数券・ペットスクール | `docs/plans.md` |
| スタッフ勤怠 | `docs/shift-requests.md` |
| 運用中アカウントへの接続 | `docs/switch-account.md` |
