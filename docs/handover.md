# 引き継ぎ（2店舗目の立ち上げ）

作業を別のセッション・別の担当へ引き継ぐための現在地。**日付順の記録ではなく、
「いまどこまで進んでいて、次に何をするか」だけを書く。** 終わった項目は消してよい。

サーバーのホスト名・IP・認証情報は**このファイルに書かない**（リポジトリは公開設定のため）。
引き継ぎ時に別途伝える。

> **リポジトリの移管（2026-08）**: FREE WAN 店（2店舗目）のアプリは
> **emcyrup/freewan で管理する**ことになった。デプロイの Secrets もこのリポジトリに置く。
> cocotte-vert（1号店）は元リポジトリのまま。汎用的な修正は upstream 経由で相互に取り込む
> （README の手順参照）。
>
> これに伴うサーバー側の切り替え（1回だけ・データは消えない）:
>
> ```bash
> cd ~/cocotte-vert
> git remote set-url origin https://github.com/emcyrup/freewan.git
> git fetch origin && git checkout main && git reset --hard origin/main
> docker compose --profile standalone up -d --build
> ```
>
> ディレクトリ名は `cocotte-vert` のままでよい（CI は freewan / cocotte-vert 両対応）。
> Docker のボリューム名はディレクトリ名から決まるため、**ディレクトリを rename しないこと**
> （rename すると新しい空の DB で起動してしまう）。

---

## 1. 2店舗目のサーバー構築

GCP に VM を1台立て、`docs/new-store.md` の手順で構築中。

| Step | 内容 | 状態 |
|---|---|---|
| 1 | VM を用意（固定 IP・ポート80/443 開放） | **完了** |
| 2 | ドメインを DNS で VM に向ける（DuckDNS） | **完了** |
| 3 | Docker とコードを入れる | **完了** |
| 4 | `.env` を作る | **完了**（LINE 本物の値に差し替え済み） |
| 5 | 起動して HTTPS を確認・管理画面が開く | **完了** |
| 6 | LINE チャネル・LIFF を作り Webhook を設定 | **ほぼ完了**（2026-08-13 check-line.js 全項目OK: Bot応答・Webhook検証200・LIFF設定済み。残: リッチメニュー登録と、スタッフ用 LINE グループに Bot を招待） |
| 7 | 初期データ投入（メニュー・コース・スタッフ・既存顧客） | **進行中**（2026-08-13 メニュー9件・スクール会員コース2件を投入済み。残: スタッフ登録と LINE 連携、既存顧客台帳、`.env` の `STORE_*`） |
| 8 | 自動デプロイを2台目に対応させる | **完了**（2026-08-13 疎通確認済み。main への push で自動反映される） |

### Step 6 の残り

1. リッチメニュー登録（`scripts/setup-richmenu.js`。画像 2500×843px が要る）
2. スタッフ用 LINE グループを作り Bot を招待する（`STAFF_NOTIFY_CHANNEL=line` のため、
   招待するまでスタッフ通知の届き先が無い。招待すると通知先グループが自動設定される）
3. 現在のチャネルは表示名「freewanテスト」。本番の公式アカウントに切り替える際は
   `docs/switch-account.md` の手順で（**同一プロバイダー内**でないと友だちの紐付けが切れる点に注意）
4. 通数は無料枠 200通/月。live 切り替え前に想定通数を試算してプランを上げる

### Step 7 の残り

- メニュー・コースは投入済み（所要時間はモック段階の仮値。ヒアリング後に
  `scripts/store-data/freewan.menus.json` を直して再実行。同名はスキップされるので何度でも安全）
- スタッフを管理画面から登録し、LINE 連携する（`docs/shift-requests.md`）
- **既存顧客台帳（氏名・電話番号）の投入が最重要。** LIFF 登録時の突合率に直結する
- `STORE_*` の残り（営業時間・定休日・住所・電話・サブタイトル）を `.env` に追記する。
  未設定だと現行店舗の既定値が出る

### Step 8 でやること（残りは Secrets 登録のみ）

ci.yml は複数台対応済み。`DEPLOY_TARGETS`（`user@host` の改行区切り）で全店舗へ配る。
残作業（リポジトリの Settings → Secrets and variables → Actions）:

1. 1台目と同じ deploy_key の**公開鍵を2台目にも登録**（GCP なので `authorized_keys` 直接追記ではなく
   **インスタンスのメタデータ**に追加する。手順と注意は docs/deploy.md「GCP では〜」参照）
2. Secret `DEPLOY_TARGETS` に2台ぶんの `user@host` を改行区切りで登録
   （設定後は旧 `VM_HOST` / `VM_USER` は使われないので消してよい）
3. main に何かをマージし、両店舗へ配られることを Actions のログで確認
   （1台の失敗で他店舗は止まらず、最後にまとめて失敗報告される）

**店舗ごとにコードを分岐させないこと。** 差分は `.env` に寄せる方針で作ってある
（`src/store.js` と配信日数の設定）。メニュー・定額コースの初期データも
`scripts/store-data/` の JSON に寄せた。どうしても吸収できない差が出たら、まず設定化を検討する。

---

## 2. 本稼働までの残り作業（北区店の要件 R1〜R9 に対して）

> **番号の読み替えに注意**: 要件書（`docs/osaka-kitaku/要件定義書.md`）の R1〜R9 と、
> システム内部の設定キー（`src/reminders.js` の R1〜R4 = preReminder / afterVisit / dormant / birthday）は
> **別の番号体系**。この表は**要件書の番号を正**とする。

### 配信（リマインド）

| 要件 | システム側の状態 |
|---|---|
| R1 前々日確認 | **実装済み**（preReminder。店舗・お客様単位の ON/OFF 付き） |
| R2 誕生日祝い | **実装済み**（birthday。クーポンは `BIRTHDAY_COUPON_URL`） |
| R3 ワクチン更新案内 | **未**。ワクチン情報の保持（データ設計）ごと未着手 |
| R4 体験3日後の体調伺い | **部分**。afterVisit の日数は `.env` で 7→3 にできるが、「体験利用のみ対象」の絞りが無い |
| R5 体験後の追客（3パターン） | **未**（入園状況のステータス管理ごと未着手） |
| R6 回数券の来店促し | **未**（回数管理は実装済み。配信条件が未 → 次に着手 2.） |
| R7 会員の残回数案内 | **未**（同上） |
| R8 持ち越し分の消化案内 | **未** |
| R9 来店お礼（写真付き） | **未**。LINE は1通=吹き出し5個（文＋写真4枚）で課金1通分。写真の署名付き URL 配信が要る |
| R10 休眠フォロー（90日） | **実装済み・採用決定**（2026-08-14。要件書に R10 として追記済み。管理画面の ON/OFF で制御） |
| スタッフ確認付き送信 | **未**（試験運用 Phase 5 の前までに必要） |

### 配信以外

| 要件 | 状態 |
|---|---|
| 顧客管理・カルテ・来店履歴 | 実装済み |
| シフト管理＋LINE 申請 | 実装済み |
| 回数券・会員管理 | 実装済み（`docs/plans.md`）。**来店登録との連動も実装済み**（2026-08-14。予約に `pet_id` を持ち、visited で自動消化・取り消しで復元。対象メニューは `menus.consumes` で設定） |
| 予約カレンダー（日/週/月・ドラッグコピー・両端リサイズ・7:00〜22:00 表示・時間帯色分け・新規+30分枠） | **未**（画面はサンプルのまま。仕様は要件書 2.2 と `docs/osaka-kitaku/mock/` を正とする） |
| 新人スタッフの⭐マーク | **未**（モック画面のみ。実装の staff には無い） |
| EPARK メール自動取込 | 未。Phase 1 で実物を確認してから着手（それまで工数が読めない） |
| SNS 2分割投稿 | 実装済み（Instagram・スレッズ） |
| リール投稿 | 未 |
| ダッシュボード（入園数・体験入園一覧・トリマー実績・来店経路） | 部分（トリマー実績は担当者データ、入園数は回数券から出せる。体験一覧・来店経路は未） |
| 顧客ごとのリマインド設定 | 実装済み（実装済みジョブぶんのみ。R3〜R9 の実装時に対象を広げる） |

### 次に着手すべきもの

1. R6 / R7 の配信条件（来店連動で残回数が正しく減るようになったので組める）
2. リマインドのスタッフ確認付き送信

（済）来店登録と回数消化の連動 — 2026-08-14 実装。予約フォーム・手入力・取り込み API とも
「どの子か」を受け取り、visited への変更で `menus.consumes`（plan/ticket）に応じて1回消化。
残0で消化できないときは、コース加入中の場合のみスタッフへ通知。来店の取り消しで消化も戻る。
マイグレーション 011 はコンテナ起動時に自動適用される。残る手作業は
`seed-menus.js --file=...` の再実行（メニューへの consumes 反映）のみ。

### 計画に抜けていたもの（2026-08-13 の監査で判明・上表に反映済み）

- **R9 来店お礼（写真付き）が残作業一覧に載っていなかった**
- ワクチン更新案内を「R8」と書いていた（要件書では R3。R8 は持ち越し消化案内）
- 画面系の要件（新人⭐・新規+30分枠・時間帯色分け・日/週/月表示）が未追跡だった
- 継続中の既知の指摘: ワクチン管理が Phase 3 の項目に無い / Phase 2 の API 申請に
  **スレッズ**が含まれていない（Instagram とは別トークンが要る）

---

## 3. 店舗へ確認したい運用ルール

回数券・保育コースは現行モックの表示に合わせて実装してある。店舗ごとに違いうるので、
ヒアリング（Phase 1）で確認する。変更はいずれも1箇所で済むようにしてある。

- 消化の順番は**当月分優先**でよいか（繰越分が失効しやすくなる）
- 繰越は**1ヶ月**でよいか
- 月途中の加入は**満額**か日割りか（いまは満額）
- 解約後の残回数は**期限まで使えるまま**でよいか
- 早朝・夜間の基準時刻（モックでは 9:00 / 18:00 と仮置き）と早朝・夜間料金の有無
- 回数消化の対象メニュー（現在: 幼稚園（スクール）=コース消化、シャンプーコース=回数券消化。
  `scripts/store-data/freewan.menus.json` の `consumes` で変更できる）

---

## 4. 引き継ぐときに必ず伝えること

このファイルに書かない情報。口頭か別の安全な経路で渡す。

- 2店舗目のドメイン・外部 IP・SSH のユーザー名
- 管理画面の `ADMIN_USER` / `ADMIN_PASSWORD`
- LINE / Instagram / Threads / Anthropic の各トークン
- `POSTGRES_PASSWORD`

---

## 5. 作業を引き継いだ人が最初に読むもの

| | |
|---|---|
| 開発の約束ごと | `CLAUDE.md`（**誤爆防止のルールは必ず読む**） |
| 全体像 | `README.md` |
| 別店舗への展開手順 | `docs/new-store.md` |
| サーバー構築 | `docs/deploy.md` |
| 画面の切り分け（実運用／デモ） | `src/mock/README.md` |
| 回数券・保育コース | `docs/plans.md` |
| スタッフ勤怠 | `docs/shift-requests.md` |
| 運用中アカウントへの接続 | `docs/switch-account.md` |
